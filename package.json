{
  "name": "shades-app",
  "version": "1.0.0",
  "description": "A machine learning app that uses TensorFlow.js, React, and Parcel to best match makeup foundation shades from various beauty brands to images of skin tones.",
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "install-backend": "cd backend && npm install",
    "prestart": "npm run install-frontend && npm run install-backend",
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-backend": "cd backend && node app.js",
    "start-frontend": "cd frontend && npm start"
  },
  "author": "Asim Ali",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
